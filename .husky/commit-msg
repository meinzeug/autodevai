# AutoDev-AI Commit Message Hook

# Validate commit message with commitlint (skip if not installed)
# npx --no-install commitlint --edit "$1" 2>/dev/null || true

# Store commit info in Claude Flow memory
npx claude-flow@alpha hooks post-commit --message "$(cat $1)"